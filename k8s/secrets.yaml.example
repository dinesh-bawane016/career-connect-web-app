# This is an EXAMPLE file. DO NOT commit this to Git with real credentials!
# Copy this to secrets.yaml and fill in your actual values

apiVersion: v1
kind: Secret
metadata:
  name: server-secret
type: Opaque
stringData:
  # MongoDB Connection String
  # Replace with your actual MongoDB Atlas connection string
  MONGO_URI: "mongodb+srv://dineshUser:dinesh%40123@cluster0.r3kgsel.mongodb.net/careerconnect?retryWrites=true&w=majority&appName=Cluster0"
  
  # JWT Secret for authentication
  # Use a strong, random string in production
  JWT_SECRET: "fgjfgsudgfudnhfousidfnewuikfmlewf"
  
  # Cloudinary credentials for image uploads
  CLOUDINARY_CLOUD_NAME: "dtgsdprcl"
  CLOUDINARY_API_KEY: "819224586478434"
  CLOUDINARY_API_SECRET: "yr_TbbT_JDG2Xfu4KcrrcykeJvA"

---
apiVersion: v1
kind: Secret
metadata:
  name: nexus-secret
type: kubernetes.io/dockerconfigjson
data:
  # This is base64 encoded Docker config
  # Create this using:
  # kubectl create secret docker-registry nexus-secret \
  #   --docker-server=nexus-service-for-docker-hosted-registry.nexus.svc.cluster.local:8085 \
  #   --docker-username=admin \
  #   --docker-password=Changeme@2025
  .dockerconfigjson: ""

# To create secrets from this file:
# 1. Copy this file: cp secrets.yaml.example secrets.yaml
# 2. Edit secrets.yaml with your actual values
# 3. Apply: kubectl apply -f secrets.yaml
# 4. Verify: kubectl get secrets
